[project]
name = "codee-tasks-jira"
requires-python = ">=3.12"
version = "0.1.0"
dependencies = [
    "codee-tasks-abstract",
    "codee-main-context",
    "requests >= 2.31,<3",
]

[project.scripts]
test = "codee_tasks_jira:test.main"

[tool.uv.sources]
codee-tasks-abstract = { workspace = true }
codee-main-context = { workspace = true }
